{
  "name": "console",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@console/web",
    "dev:api": "npm run dev --workspace=@console/api",
    "dev:all": "npm run dev:api & npm run dev",
    "build": "npm run build --workspace=@console/web",
    "start:api": "npm run start --workspace=@console/api",
    "lint": "npm run lint --workspace=@console/web"
  }
}
